services:
  traefik:
    image: traefik:latest
    container_name: traefik
    command:
      #- "--log.level=DEBUG"
      - "--log.level=ERROR"
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.linode.acme.dnschallenge=true"
      - "--certificatesresolvers.linode.acme.dnschallenge.delayBeforeCheck=60"
      - "--certificatesresolvers.linode.acme.dnschallenge.disablePropagationCheck=true"
      - "--certificatesresolvers.linode.acme.dnschallenge.provider=linodev4"
      - "--certificatesresolvers.linode.acme.email=${TRAEFIK_EMAIL}"
      - "--certificatesresolvers.linode.acme.storage=/data/acme.json"
    environment:
      - LINODE_TOKEN=${TRAEFIK_LINODE_TOKEN}
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DATA_PREFIX}/traefik:/data
    labels:
      - "traefik.http.routers.http-catchall.rule=hostregexp(`{host:.+}`)"
      - "traefik.http.routers.http-catchall.entrypoints=web"
      - "traefik.http.routers.http-catchall.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"